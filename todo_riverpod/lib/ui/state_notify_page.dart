import 'package:flutter/material.dart';

class StateNotifyPage extends StatelessWidget {
  const StateNotifyPage({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('StateNotifyPage'),
      ),
      body: Center(
        child: Text('StateNotifyPage'),
      ),
    );
  }
}
